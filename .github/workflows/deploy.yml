name: Deploy Paper

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: impact-scholars/isp-actions-config/.github/workflows/deploy-paper.yml@v0.1.0
    with:
      base_url: /${{ github.event.repository.name }}
      # paper-environment-file: paper-environment.yml  # Optional: override if using different filename
